import React from 'react';
import './index.scss';

// components
import Sidebar from '../sidebar';

const Layout = () => {
  return (
    <div>
      <Sidebar />
    </div>
  );
};

export default Layout;
